#include<iostream>
using namespace std;
int main(){
    int a,b;
    cout<<" Enter the value of a: ";
    cin>>a;
    cout<<"Enter the value of b: ";
    cin>>b;
    cout<<"The right shift of a and  1(a>>b)is : "<<(a>>1)<<endl;
    cout<<"Thee left shift if a and b(a<<b) is: "<<(b<<2);
}
